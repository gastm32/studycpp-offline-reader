# 异常、类和继承

本节阅读量: 


---

## 异常和成员函数

到目前为止，在本教程中，您只看到了在非成员函数中使用的异常。然而，异常在成员函数中同样有用，在重载运算符中尤其如此。考虑以下IntArray重载「operator[]」的代码：

```
int& IntArray::operator[](const int index)
{
    return m_data[index];
}
```

只要index是有效的数组索引，该函数就可以正常工作，但它严重缺乏错误检查。我们可以添加断言语句来确保索引有效：

```
int& IntArray::operator[](const int index)
{
    assert (index >= 0 && index < getLength());
    return m_data[index];
}
```

现在，如果用户传入无效索引，程序将触发断言错误。不幸的是，由于重载运算符对参数和返回值类型有严格要求，因此很难灵活地将错误码或布尔值传递给调用方来处理。然而，异常不会更改函数签名，因此可以在这里使用。下面是一个示例：

```
int& IntArray::operator[](const int index)
{
    if (index < 0 || index >= getLength())
        throw index;

    return m_data[index];
}
```

现在，如果用户传入无效的索引，「operator[]」将抛出int异常。

---

## 当构造函数执行失败

构造函数是类中另一个适合使用异常的地方。如果构造函数由于某种原因必须失败（例如，用户传入了无效输入），只需抛出一个异常来表示对象创建失败。在这种情况下，对象的构造会被中止，并且所有类成员（在构造函数函数体执行之前已经创建和初始化）都会像往常一样被销毁。

然而，这不会调用类的析构函数（因为对象从未完成构造）。由于析构函数不会执行，因此不能依赖析构函数来清理已经分配的资源。

这带来了一个问题：如果我们在构造函数中分配了资源，然后在构造函数完成之前发生异常，应该怎么办？如何确保已经分配的资源得到适当清理？一种方法是将任何可能失败的代码包装在try块中，使用相应的catch块捕获异常并进行必要清理，然后重新引发异常（后面章节会讨论）。然而，这会增加很多混乱，并且很容易出错，特别是在类分配多个资源时。

幸运的是，有一种更好的方法。我们可以利用“即使构造函数失败，类成员也会被销毁”这一事实：如果在类成员内部（而不是构造函数本身）进行资源分配，那么当这些成员被销毁时，它们就可以完成清理。

下面是一个示例：

```
#include <iostream>

class Member
{
public:
	Member()
	{
		std::cerr << "Member allocated some resources\n";
	}

	~Member()
	{
		std::cerr << "Member cleaned up\n";
	}
};

class A
{
private:
	int m_x {};
	Member m_member;

public:
	A(int x) : m_x{x}
	{
		if (x <= 0)
			throw 1;
	}
	
	~A()
	{
		std::cerr << "~A\n"; // 不会被调用
	}
};


int main()
{
	try
	{
		A a{0};
	}
	catch (int)
	{
		std::cerr << "Oops\n";
	}

	return 0;
}
```

这会打印：

```
Member allocated some resources
Member cleaned up
Oops
```

在上面的程序中，当类A抛出异常时，A的所有成员都会被销毁。m\_member的析构函数会被调用，从而获得清理其分配资源的机会。

这也是RAII如此受欢迎的部分原因——即使在特殊情况下，实现RAII的类也能够在自身被销毁时完成清理。

然而，创建一个自定义类（如Member）来管理资源分配并不总是高效。幸运的是，C++标准库提供了符合RAII的类，用于管理常见资源类型，例如文件（std::fstream，下一章介绍）和动态内存（std::unique\_ptr及其他智能指针）。

例如，不是这样：

```
class Foo
private:
    int* ptr; // Foo 需要处理内存分配和释放
```

而应这样做：

```
class Foo
private:
    std::unique_ptr<int> ptr; // std::unique_ptr 来处理内存分配和释放
```

在前一种情况下，如果Foo的构造函数在ptr分配动态内存后失败，则Foo必须负责清理，这可能很有挑战。在后一种情况下，如果Foo的构造函数在ptr分配动态内存后失败，ptr的析构函数会执行并将该内存归还给系统。将资源处理委托给符合RAII的成员时，Foo不必执行任何显式清理！

---

## 异常类

使用基本数据类型（如int）作为异常类型的主要问题之一是，它们本质上含义模糊。更大的问题是，当一个try块中有多个语句或函数调用时，异常的含义会产生歧义。

```
// 使用上面重载 operator[] 的 IntArray

try
{
    int* value{ new int{ array[index1] + array[index2]} };
}
catch (int value)
{
    // 这里捕获了什么?
}
```

在这个例子中，如果我们捕获一个int异常，它到底告诉了我们什么？数组索引之一是否超出边界？运算符+是否导致整数溢出？new是否因内存不足而失败？不幸的是，在这种情况下，没有简单的方法可以消除歧义。虽然我们可以抛出const char \*异常来帮助识别错误，但这仍然不能让我们以不同方式处理来自不同来源的异常。

解决这个问题的一种方法是使用异常类。异常类只是一个普通类，专门设计为作为异常抛出。让我们设计一个简单的异常类，与IntArray类一起使用：

```
#include <string>
#include <string_view>

class ArrayException
{
private:
	std::string m_error;

public:
	ArrayException(std::string_view error)
		: m_error{ error }
	{
	}

	const std::string& getError() const { return m_error; }
};
```

下面是使用该类的完整程序：

```
#include <iostream>
#include <string>
#include <string_view>

class ArrayException
{
private:
	std::string m_error;

public:
	ArrayException(std::string_view error)
		: m_error{ error }
	{
	}

	const std::string& getError() const { return m_error; }
};

class IntArray
{
private:
	int m_data[3]{}; // 简单起见，这里先固定长度为3

public:
	IntArray() {}

	int getLength() const { return 3; }

	int& operator[](const int index)
	{
		if (index < 0 || index >= getLength())
			throw ArrayException{ "Invalid index" };

		return m_data[index];
	}

};

int main()
{
	IntArray array;

	try
	{
		int value{ array[5] }; // 超出数组范围
	}
	catch (const ArrayException& exception)
	{
		std::cerr << "An array exception occurred (" << exception.getError() << ")\n";
	}
}
```

使用这样的类，我们可以让异常返回问题描述，为错误提供上下文信息。由于ArrayException是独立的类型，因此我们可以专门捕获IntArray类引发的异常，并根据需要将它们与其他异常区别对待。

请注意，异常处理程序应该通过引用而不是按值捕获类异常对象。这可以防止编译器在捕获异常的位置复制该异常。当异常是类对象时，拷贝对象可能非常昂贵。同时，这也可以防止在处理派生异常类（我们稍后会讨论）时发生对象切片。通常应避免通过指针捕获异常，除非您有特定原因这样做。

**最佳实践**

基本类型的异常可以被值捕获，因为它们的复制成本很低。类类型的异常应由（const）引用捕获，以防止昂贵的复制和切片。

---

## 异常和继承

由于可以将类作为异常抛出，并且类可以从其他类派生，因此我们需要考虑将继承类用作异常时会发生什么。事实证明，异常处理程序不仅会匹配特定类型的类，还会匹配从该特定类型派生的类！考虑以下示例：

```
#include <iostream>

class Base
{
public:
    Base() {}
};

class Derived: public Base
{
public:
    Derived() {}
};

int main()
{
    try
    {
        throw Derived();
    }
    catch (const Base& base)
    {
        std::cerr << "caught Base";
    }
    catch (const Derived& derived)
    {
        std::cerr << "caught Derived";
    }

    return 0;
}	
```

在上面的例子中，我们抛出了一个Derived类型的异常。然而，该程序的输出是：

```
caught Base
```

发生了什么？

首先，如上所述，派生类会被基类的处理程序捕获。因为Derived是从Base派生而来的，所以Derived是一个Base（它们具有is-a关系）。其次，当C++试图为引发的异常查找处理程序时，会按顺序查找。因此，C++做的第一件事是检查Base类的异常处理程序是否与Derived异常匹配。因为Derived是一个Base，所以答案是yes，于是它执行Base类型的catch块！在这种情况下，Derived的catch块甚至从未被检查到。

为了使此示例按预期工作，我们需要翻转catch块的顺序：

```
#include <iostream>

class Base
{
public:
    Base() {}
};

class Derived: public Base
{
public:
    Derived() {}
};

int main()
{
    try
    {
        throw Derived();
    }
    catch (const Derived& derived)
    {
        std::cerr << "caught Derived";
    }
    catch (const Base& base)
    {
        std::cerr << "caught Base";
    }

    return 0;
}	
```

这样，Derived处理程序在捕获Derived类型对象时就会获得优先处理机会（位于Base处理程序之前）。

如果异常对象是Base，或者是Base的其他派生类，那么它不会被Derived类型的catch捕获。程序会继续向下查找，走到Base类型的catch处理程序，然后进行处理。

使用基类异常处理程序捕获派生类型异常的能力非常有用。

**规则**

派生类的异常处理catch应放在基类之前。

---

## std::exception

标准库中的许多类和运算符在失败时会引发异常类。例如，如果运算符new无法分配足够的内存，则它可以抛出std::bad\_alloc。失败的dynamic\_cast会引发std::bad\_cast。从C++20开始，有28个不同的异常类可以抛出，并且每个后续语言标准中都添加了更多异常类。

好消息是，所有这些异常类都派生自一个名为std::exception的类（在<exception>头文件中定义）。exception是一个小型接口类，旨在充当C++标准库引发的所有异常的基类。

在大多数情况下，当标准库抛出异常时，我们并不关心它是错误的分配、错误的强制转换，还是其他问题。由于所有标准库异常都是std::exception的子类，我们可以设置一个异常处理程序来捕获std::exception类型的异常，这样就可以处理所有标准库抛出的异常。很简单！

```
#include <cstddef> // for std::size_t
#include <exception> // for std::exception
#include <iostream>
#include <limits>
#include <string>

int main()
{
    try
    {
        // 我们的代码这里是用了标准库
        // 一个引发异常的样例
        std::string s;
        s.resize(std::numeric_limits<std::size_t>::max()); // 会触发 std::length_error 或者 内存分配异常
    }
    // 这里会 catch 住 std::exception 和所有对应的派生类
    catch (const std::exception& exception)
    {
        std::cerr << "Standard exception: " << exception.what() << '\n';
    }

    return 0;
}
```

在作者的机器上，上述程序打印：

```
Standard exception: string too long
```

上面的例子应该非常简单。值得注意的一点是，std::exception有一个名为what()的虚成员函数，该函数返回异常的C样式字符串描述。大多数派生类都会重写what()函数来更改消息。注意，该字符串仅用于描述性文本——不要将其用于比较，因为不同编译器并不保证同一异常返回相同字符串。

有时，我们希望以不同方式处理特定类型的异常。在这种情况下，我们可以为该特定类型添加处理程序，并让所有其他异常“落到”基础处理程序。考虑：

```
try
{
     // 使用标准库的代码
}
// 这里会 catch std::length_error (和任何它的派生类)
catch (const std::length_error& exception)
{
    std::cerr << "You ran out of memory!" << '\n';
}
// 这里会 catch std::exception (以及非std::length_error)
catch (const std::exception& exception)
{
    std::cerr << "Standard exception: " << exception.what() << '\n';
}
```

在本例中，std::length\_error类型的异常会由第一个处理程序捕获并处理。第二个处理程序会捕获std::exception类型的异常以及所有其他派生类异常。

这样的继承层次结构允许我们使用特定处理程序定位特定的派生异常类，或者使用基类处理程序捕获整个异常层次结构。这让我们可以在一定程度上控制想要处理的异常类型，同时确保不必做太多工作就能捕获层次结构中的“其他一切”。

---

## 直接使用标准异常

标准库中没有任何东西会直接抛出std::exception，您也不应该这样做。不过，如果标准异常类足以表达您的需求，那么您可以按需抛出它们。您可以在cppreference上找到所有标准异常的列表。

std::runtime\_error（包含在stdexcept头文件中）是一个常见选择，因为它名称通用，并且其构造函数接受可自定义的消息：

```
#include <exception> // for std::exception
#include <iostream>
#include <stdexcept> // for std::runtime_error

int main()
{
	try
	{
		throw std::runtime_error("Bad things happened");
	}
	// 这里会 catch std::exception 及其所有派生类
	catch (const std::exception& exception)
	{
		std::cerr << "Standard exception: " << exception.what() << '\n';
	}

	return 0;
}
```

这会打印：

```
Standard exception: Bad things happened
```

---

## 从std::exception或std::runtime\_error派生您自己的类

当然，您可以从std::exception派生自己的类，并重写const虚函数what()。下面是与上面相同的程序，其中ArrayException派生自std::exception。

```
#include <exception> // for std::exception
#include <iostream>
#include <string>
#include <string_view>

class ArrayException : public std::exception
{
private:
	std::string m_error{}; // 我们自己保存的错误

public:
	ArrayException(std::string_view error)
		: m_error{error}
	{
	}

	// std::exception::what() 返回 const char*, 需要保持一致
	const char* what() const noexcept override { return m_error.c_str(); }
};

class IntArray
{
private:
	int m_data[3] {}; // 简单起见，这里先固定长度为3

public:
	IntArray() {}
	
	int getLength() const { return 3; }

	int& operator[](const int index)
	{
		if (index < 0 || index >= getLength())
			throw ArrayException("Invalid index");

		return m_data[index];
	}

};

int main()
{
	IntArray array;

	try
	{
		int value{ array[5] };
	}
	catch (const ArrayException& exception) // 派生类优先处理
	{
		std::cerr << "An array exception occurred (" << exception.what() << ")\n";
	}
	catch (const std::exception& exception)
	{
		std::cerr << "Some other std::exception occurred (" << exception.what() << ")\n";
	}
}
```

请注意，虚函数what()具有noexcept说明符（这意味着函数承诺自身不抛出异常）。因此，我们的重写版本也应该具有noexcept说明符。

由于std::runtime\_error已经具有字符串处理功能，因此它也是异常派生类的常见基类。std::runtime\_error可以接收C样式字符串参数，也可以接收「const std::string&」参数。

下面是从std::runtime\_error派生的相同示例：

```
#include <exception> // for std::exception
#include <iostream>
#include <stdexcept> // for std::runtime_error
#include <string>

class ArrayException : public std::runtime_error
{
public:
	// std::runtime_error 	接收 const char* 或 const std::string&.
	// 我们这里接收 const std::string&
	ArrayException(const std::string& error)
		: std::runtime_error{ error } // std::runtime_error 会处理这个string
	{
	}


        // 没有必要 重写 what()，因为可以直接使用 std::runtime_error::what()
};

class IntArray
{
private:
	int m_data[3]{}; // 简单起见，这里先固定长度为3

public:
	IntArray() {}

	int getLength() const { return 3; }

	int& operator[](const int index)
	{
		if (index < 0 || index >= getLength())
			throw ArrayException("Invalid index");

		return m_data[index];
	}

};

int main()
{
	IntArray array;

	try
	{
		int value{ array[5] };
	}
	catch (const ArrayException& exception) // 派生类优先处理
	{
		std::cerr << "An array exception occurred (" << exception.what() << ")\n";
	}
	catch (const std::exception& exception)
	{
		std::cerr << "Some other std::exception occurred (" << exception.what() << ")\n";
	}
}
```

您可以决定是创建自己的独立异常类、使用标准异常类，还是从std::exception或std::runtime\_error派生自己的异常类。这些都是有效方法，具体取决于您的目标。

---

## 异常的生命周期

当抛出异常时，被抛出的对象通常是在堆栈上分配的临时变量或局部变量。然而，异常处理过程可能会展开函数栈，导致函数的所有局部变量被销毁。那么，被抛出的异常对象如何在栈展开中存活下来呢？

当抛出异常时，编译器会将异常对象的副本复制到某块为处理异常而保留的未指定内存中（位于调用栈之外）。这样，无论堆栈是否展开、展开多少次，异常对象都会被持久保存，确保在处理异常之前一直存在。

这意味着被抛出的对象通常需要是可复制的（即使栈实际上没有展开）。高度优化的编译器可以改为执行对象移动，或者在特定情况下完全消除副本创建。

下面的示例展示了当我们尝试抛出不可复制的派生对象时会发生什么：

```
#include <iostream>

class Base
{
public:
    Base() {}
};

class Derived : public Base
{
public:
    Derived() {}

    Derived(const Derived&) = delete; // 不可复制
};

int main()
{
    Derived d{};

    try
    {
        throw d; // 编译失败: Derived 拷贝构造函数被删除
    }
    catch (const Derived& derived)
    {
        std::cerr << "caught Derived";
    }
    catch (const Base& base)
    {
        std::cerr << "caught Base";
    }

    return 0;
}
```

编译此程序时，编译器会报错，指出Derived拷贝构造函数不可用，并停止编译。

异常对象不应保存指向栈上分配对象的指针或引用。如果引发的异常导致函数调用栈展开（从而销毁栈上分配的对象），这些指针或引用就会悬空。

**提示**

异常对象需要是可复制的。

---



---

![](/images/zan.jpg)


---