# C++ 综合测试卷（1-20章综合评估）

**考试范围�?* C++基础知识、函数及重载模板、数据类型、运算符、控制流、作用域、指针引用复合类型、类与对象、STL容器及算法、动态内存分配�?**难度级别�?* 中等~偏难（对标名企笔试难度）
**总分�?* 100�?
---

## 一�?单项选择题（每题 3 分，�?30 分）

**1. [作用域与编译链接] 关于 C++ 中的 `static` 关键字，以下说法错误的是�?*
- A. 在函数内部定义的静态局部变量，其生命周期贯穿整个程序运行期�?- B. 全局静态变量的作用域仅限于定义它的源文件内（内部链接属性）�?- C. 类的静态成员函数中不能直接访问非静态成员变量�?- D. `static` 修饰的成员变量在类定义时可以被自动初始化，不需要在类外单独定义�?> **您的答案�?* _C，我不确定_________________

**2. [类型推导与别名] 对于以下代码，`decltype(x)` �?`auto` 的推导结果是什么？**
```cpp
const int a = 10;
const int& b = a;
auto x = b;
```
- A. `x` 的类型是 `const int&`
- B. `x` 的类型是 `const int`
- C. `x` 的类型是 `int`
- D. `x` 的类型是 `int&`
> **您的答案�?* A

**3. [引用与指针] 以下关于指针和引用的描述，哪一项是不正确的�?*
- A. 引用在声明时必须初始化，而指针可以先声明后初始化�?- B. 存在指向指针的指针，但也存在指向引用的引用（Reference to reference）�?- C. 指针可以�?`nullptr`，但引用必须绑定到一个合法的对象上�?- D. 指针可以随时改变所指对象，但引用一旦绑定，无法改变�?> **您的答案�?* A不懂，B不知道，C\D对的

**4. [类与对象] 下列关于 C++ 类的构造函数和析构函数的说法，正确的是�?*
- A. 构造函数可以被声明为虚函数（virtual）�?- B. 如果类中包含动态分配的内存，通常需要自定义拷贝构造函数和赋值运算符（Rule of Three/Five）�?- C. 析构函数可以重载，以便根据不同的条件释放资源�?- D. 如果没有显式定义任何构造函数，编译器总是会生成一个无参的默认构造函数，即使类中�?`const` 成员变量未初始化�?> **您的答案�?* A有点忘了，但是应该不是答案。C错误吧。D__________________

**5. [动态数组与固定数组] 对比 `std::vector` �?`std::array`，以下哪种说法是正确的？**
- A. `std::array` �?`std::vector` 的大小都可以在运行时动态改变�?- B. `std::vector` 在进�?`push_back` 导致扩容时，一定会导致原有迭代器失效�?- C. `std::array` 的数据内存分配在堆（Heap）上，�?`std::vector` 的数据内存在栈（Stack）上�?- D. 访问 `std::vector` 中超出范围的元素时，使用 `[]` 运算符会自动抛出 `std::out_of_range` 异常�?> **您的答案�?* 都是错的吧__________________

**6. [函数重载与模板] 考虑以下函数重载和模板代码，调用 `func(5)` 时，编译器会如何选择�?*
```cpp
void func(int x) { /* 1 */ }
template <typename T> void func(T x) { /* 2 */ }
```
- A. 调用 2，因为模板匹配更加泛型�?- B. 调用 1，当普通函数和模板都能完全匹配时，编译器优先选择普通函数�?- C. 编译报错，产生二义性（Ambiguous call）�?- D. 调用 2，因为常�?`5` 被推导为更宽泛的类型�?> **您的答案�?* B

**7. [迭代器与算法] 使用 `<algorithm>` 中的 `std::sort` �?`std::list` 进行排序是否合法�?*
- A. 合法，`std::sort` 适用于所有标准库容器�?- B. 不合法，`std::sort` 需要随机访问迭代器（Random Access Iterator），�?`std::list` 只提供双向迭代器�?- C. 合法，但效率很低，应该自己手写排序�?- D. 不合法，`std::list` 不允许修改元素的值�?> **您的答案�?* __________________不知道，关于容器，还得给我上一�?
**8. [动态内存分配] 关于 `new` �?`malloc` 的区别，下列说法错误的是�?*
- A. `new` 会调用对象的构造函数，�?`malloc` 仅分配内存不调用构造函数�?- B. `new` 返回带有类型的指针，`malloc` 返回 `void*` 需要强转�?- C. `new` 分配内存失败时默认抛�?`std::bad_alloc` 异常，�?`malloc` 返回 `NULL`�?- D. 可以使用 `free` 来释�?`new` 申请的内存空间，效果�?`delete` 完全一样�?> **您的答案�?* __________________D

**9. [类型转换] 想要在一个多态的继承体系中，将一个基类指针安全地向下转换（Downcast）为派生类指针，应该使用哪种转型操作符？**
- A. `static_cast`
- B. `dynamic_cast`
- C. `const_cast`
- D. `reinterpret_cast`
> **您的答案�?* _A_________________

**10. [控制流与作用域] 下面这段代码的输出结果是什么？**
```cpp
#include <iostream>
int count = 1;
void doSomething() {
    int count = 2;
    {
        int count = 3;
        std::cout << ::count << " ";
    }
    std::cout << count << "\n";
}
int main() {
    doSomething();
    return 0;
}
```
- A. 3 2
- B. 1 3
- C. 1 2
- D. 2 2
> **您的答案�?* 整个代码真的没有问题嘛？A__________________

---

## 二�?多项选择题（每题 4 分，�?20 分。漏选得 2 分，错选得 0 分）

**11. [类与对象] C++11 引入�?`= default` �?`= delete`。以下关于它们的说法正确的是�?*
- A. 可以使用 `= delete` 禁用类的拷贝构造函数，从而实现防拷贝的类（Non-copyable）�?- B. 使用 `= default` 可以要求编译器显式生成默认的无参构造函数或析构函数�?- C. `= delete` 只能用于类的特殊成员函数，不能用于普通的成员函数�?- D. 如果类中定义了带参数的构造函数，可以通过声明 `ClassName() = default;` 来显式要求编译器生成默认构造函数�?> **您的答案�?* __________________

**12. [动态内存] 为了避免内存泄漏，现�?C++ 提倡使�?RAII（资源获取即初始化）和智能指针。以下关于动态内存管理的说法，哪些是正确的？**
- A. 每次使用 `new` 分配内存后，都必须在合适的地方对应使用 `delete` 释放�?- B. `new[]` 分配的数组空间，如果使用 `delete` （而非 `delete[]`）释放，对于带有非平凡析构函数的对象会导致未定义行为（通常是部分对象未被析构）�?- C. 悬空指针（Dangling Pointer）是指向曾经被分配过但已经被释放的内存的指针�?- D. 只要逻辑严密，在代码中使用裸指针（Raw Pointer）并手动 delete 是绝对安全的，没有异常安全的隐患�?> **您的答案�?* __________________

**13. [引用与常量] 关于 `const` 和指�?引用的结合，以下声明正确的有�?*
- A. `const int* p;` 表示 `p` 是一个指向常量的指针，不能通过 `p` 修改其指向的值，�?`p` 本身的指向可以变�?- B. `int* const p;` 表示 `p` 是一个常量指针，指针 `p` 本身的指向不能被改变�?- C. `const int& r = 10;` 是合法的，因�?const 引用可以绑定到临时对象（右值）�?- D. `int& r = 10;` 也是合法的，普通引用也可以绑定到临时对象�?> **您的答案�?* AB__________________

**14. [函数参数传递] �?C++ 中向函数传递大型对象时，为了提高效率且保证对象在函数内不被修改，可以采取什么传递方式？**
- A. 值传递（Pass by value�?- B. 常量引用传递（Pass by const reference�?- C. 指向常量的指针传递（Pass by pointer to const�?- D. 普通引用传递（Pass by reference�?> **您的答案�?* BD__________________

**15. [数组与算法] 以下关于 `std::vector` �?`std::array` 及其迭代器的说法正确的是�?*
- A. `std::vector::capacity()` 始终等于 `std::vector::size()`�?- B. 遍历容器时，基于范围�?for 循环（Range-based for loop）其底层实现依赖于迭代器 `begin()` �?`end()`�?- C. `std::vector` 的数据在内存中是连续存储的，所以支持指针的算术操作和快速的随机访问�?- D. 可以使用 `<algorithm>` 库中�?`std::find` �?`std::array` �?`std::vector` 中查找指定元素�?> **您的答案�?* BCD__________________

---

## 三�?代码阅读与分析题（每�?8 分，�?16 分）

**16. 阅读以下代码并写出其输出，解释为什么�?*
```cpp
#include <iostream>

void passByValue(int* p) {
    p = new int(10);
}

void passByRef(int*& p) {
    p = new int(20);
}

int main() {
    int* ptr1 = nullptr;
    int* ptr2 = nullptr;
    
    passByValue(ptr1);
    if (ptr1 == nullptr) {
        std::cout << "ptr1 is null. ";
    } else {
        std::cout << *ptr1 << ". ";
    }
    
    passByRef(ptr2);
    if (ptr2 == nullptr) {
        std::cout << "ptr2 is null." << std::endl;
    } else {
        std::cout << *ptr2 << std::endl;
    }
    
    return 0;
}
```
**答题区：**
> **输出�?* ptr1 is null. 
> 
> **解释�?* passByRef(ptr2);不成功，ptr2应该要引用左�?> 
> 
> 


**17. 以下类定义中存在至少两个严重的潜在问题。请指出并说明如何修复�?*
```cpp
class IntArray {
private:
    int* data;
    int size;
public:
    IntArray(int s) : size(s) {
        data = new int[size];
    }
    ~IntArray() {
        delete data; // 【提示：仔细观察释放内存的方式�?    }
    int& operator[](int index) {
        return data[index];
    }
};

int main() {
    IntArray arr1(10);
    IntArray arr2 = arr1; // 发生拷贝
    arr2[0] = 5;
    return 0;
}
```
**答题区：**
> **问题1及修复：** IntArray(int s) : size(s)改成
> IntArray(int s) :    不知道，只觉得是错的
> 
> delete data;改成delete data[];
> 
> **问题2及修复：** 
> IntArray arr2 = arr1; 
> IntArray & arr2 = arr1; 
> 其实也不知道
> 

---

## 四�?简答题（每�?7 分，�?14 分）

**18. [类型推导与别名]** 请简�?`typedef` �?`using` (C++11引入) 在定义类型别名时的区别？为什么在现代 C++ 中更推荐使用 `using`？请结合模板（Template）给出一�?`using` 能做到�?`typedef` 做不到（或很别扭）的例子�?
**答题区：**
> **您的答案�?*
> 
> 
> 
> 

**19. [类与对象]** 什么是类的初始化列表（Member Initializer List）？为什么对于常量成员（`const`）或引用成员（Reference），必须使用初始化列表而不能在构造函数体中赋值？

**答题区：**
> **您的答案�?*
> 引用需要一个左值，不然不稳固�?> 
> 
> 
> 

---

## 五�?编程题（�?20 分）

**20. 综合编程题：简易购物车系统**

请使�?C++ (可以使用 `<iostream>`, `<vector>`, `<string>`, `<algorithm>`) 编写一个简易的商品和购物车系统�?**要求�?*
1. 定义一�?`Item` 结构体或类，包含商品名称（`std::string`）和商品价格（`double`）�?2. 定义一�?`ShoppingCart` 类：
   - 内部使用 `std::vector<Item>` 存储商品�?   - 提供成员函数 `void addItem(const std::string& name, double price)` 用于向购物车添加商品�?   - 提供成员函数 `double getTotalPrice() const` 用于计算并返回购物车内所有商品的总价�?   - 提供成员函数 `void sortByPrice()`，利�?`std::sort` 对购物车内的商品按价格从低到高排序�?   - 提供成员函数 `void printCart() const`，打印所有商品的名称和价格�?3. �?`main` 函数中进行测试：
   - 创建一�?`ShoppingCart` 对象�?   - 依次添加以下商品�?Apple" (3.5), "Banana" (2.0), "Milk" (5.5)�?   - 调用排序函数�?   - 打印购物车商品列表以及总价格�?
**答题区：**
> **您的代码实现�?*
> *(请在下方空行处或 markdown 代码块中编写代码)*
> 
> 
> 
> 
> 
> 



<br><br><br><br><br><br><br>

---
---

## 附：参考答案与解析（请自行核对�?
<details>
<summary>点击展开答案</summary>

### 一�?单项选择�?1. **D** (类的 static 成员变量如果不加 inline �?constexpr 修饰，必须在类外进行定义和初始化)�?2. **C** (`auto` 推导会忽略顶�?const 和引用，所�?`x` 变成了纯 `int`。`decltype` 会保留原始类�?�?3. **B** (C++ 中不存在指向引用的引用、指向引用的指针、引用的数组)�?4. **B** (A错，构造函数不能为 virtual；C错，析构函数不能重载也不能有参数；D错，有未初始化的 const 成员时，如果不提供默认初始化，编译器无法生成默认构造函�?�?5. **B** (A错，array不可变长；C错，vector的数据在堆上，array的数据通常和对象主体在一块即栈或对象内部；D错，`[]`不检查边界，`at()` 才会抛出异常)�?6. **B** (当非模板函数和模板函数同时完美匹配时，编译器优先选择普通非模板函数)�?7. **B** (`std::sort` 需�?Random Access Iterator，�?`std::list` 底层是双向链表，只提�?Bidirectional Iterator，应使用其成员函�?`list::sort()`)�?8. **D** (`delete` 会调用析构函数，`free` 不会。混�?`new` �?`free` 会导致未定义行为及资源泄�?�?9. **B** (向下转换安全的方式是 `dynamic_cast`，若转换失败对于指针会返�?`nullptr`)�?10. **C** (`::count` 访问全局�?1，花括号内的 `std::cout << ::count << " ";` 输出 `1 `。出花括号后 `std::cout << count` 访问局部变量（值为2），输出 `2`)�?
### 二�?多项选择�?11. **A, B, D** (C 错，`= delete` 可以用于普通的成员函数，例如禁止某种隐式类型转�?�?12. **A, B, C** (D 错，由于异常的存在，如果�?new �?delete 之间抛出了异常，手动 delete 将不会被执行，从而导致内存泄漏，所以提倡使�?RAII 即智能指�?�?13. **A, B, C** (D 错，普通的�?const 左值引用不能绑定到右值即临时对象�?�?14. **B, C** (两者皆可避免拷贝且防止修改，但最符合现代C++习惯的是 B：常量引�?�?15. **B, C, D** (A 错，`capacity` 代表已分配内存能容纳的元素个数，通常 >= `size`)�?
### 三�?代码阅读�?**16. 输出:** `ptr1 is null. 20`
**解释:** `passByValue` 传递的是指针的**副本**（按值传递），函数内部改变形�?`p` 的指向不影响实参 `ptr1`，因�?`ptr1` 仍为 `nullptr`。`passByRef` 传递的是指针的**引用**，形参和实参是同一个对象，在函数内分配了新内存赋值给 `p`，实质上改变了实�?`ptr2`，所�?`ptr2` 指向了新分配�?20�?
**17. 潜在问题与修�?**
- **问题1：浅拷贝导致双重释放（Double Free）与悬空指针�?* `IntArray arr2 = arr1;` 调用了默认合成的拷贝构造函数，进行指针拷贝。当 `arr1` �?`arr2` 析构时，同一�?`data` 内存被释放了两次，导致程序崩溃�?  - **修复1�?* 自定义拷贝构造函数（和赋值运算符），实现深拷�?(Deep Copy)。或者使�?`std::vector`�?- **问题2：析构函数内存释放方式错误�?* `new int[size]` 分配的是数组，应当使�?`delete[] data;` 来释放，而不�?`delete data;`�?  - **修复2�?* �?`~IntArray()` 中的 `delete data;` 修改�?`delete[] data;`�?
### 四�?简答题
**18. 区别�?*
`typedef` �?`using` 在定义普通的类型别名时等价（例如 `typedef int myInt;` �?`using myInt = int;`）。但是在现代 C++ 中，`using` 的语法更加直观，特别是结合函数指针时，可读性更好�?最重要的是，`using` 支持**模板别名（Alias Templates�?*，�?`typedef` 不支持�?例子�?```cpp
template <typename T>
using MyMap = std::map<std::string, T>; // 合法，后续可以使�?MyMap<int>

// 使用 typedef 无法直接对模板进行别名声明，必须嵌套在结构体内：
template <typename T>
struct MyMapTypedef {
    typedef std::map<std::string, T> type;
};
```

**19. 初始化列表：**
是在构造函数名和函数体之间，用冒号 `:` 开头的列表，用于在对象生成时直接初始化成员变量�?**原因�?* `const` 成员和引用成员在 C++ 中必须在声明或创建时就完成初始化。如果放在构造函数的函数�?`{}` 内，那属于“赋值（Assignment）”阶段。在这之前，成员变量已经被默认构造过了。因为常量和引用不能被重新赋值，所以在函数体内部赋值会引起编译错误。因此必须在它们被创建的时刻，也就是**初始化列�?*阶段对它们进行初始化�?
### 五�?编程题（参考实现）
```cpp
#include <iostream>
#include <vector>
#include <string>
#include <algorithm>

struct Item {
    std::string name;
    double price;
};

class ShoppingCart {
private:
    std::vector<Item> items;
public:
    void addItem(const std::string& name, double price) {
        items.push_back({name, price});
    }

    double getTotalPrice() const {
        double total = 0.0;
        for (const auto& item : items) {
            total += item.price;
        }
        return total;
    }

    void sortByPrice() {
        // 使用 Lambda 表达式定义排序规�?        std::sort(items.begin(), items.end(), [](const Item& a, const Item& b) {
            return a.price < b.price;
        });
    }

    void printCart() const {
        std::cout << "--- 购物车商品列�?---" << std::endl;
        for (const auto& item : items) {
            std::cout << item.name << " : $" << item.price << std::endl;
